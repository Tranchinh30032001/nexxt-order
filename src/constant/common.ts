export const GLOBAL_VARIABLE = {
  TIME_IS_LOGIN: 1000 * 60 * 60 * 24 * 30, // time expired is variable isLogin in coookie
  TIME_TO_REFRESH_TOKEN: 1000 * 20 // duration time to call api refreshToken
} as const
